AI Assistant 检索策略配置说明

一、检索策略总览

采用混合检索策略，结合 HyDE 假设文档嵌入和关键词检索。粗排使用向量相似度，精排使用交叉编码器重排序。最终通过 RRF 融合。

二、阈值配置

HyDE 触发阈值: 0.5。粗排 top_k=30。最终返回 top_k=3。

三、模型选择

重排序使用 BAAI/bge-reranker-base 交叉编码器。该模型在中文排序任务上表现良好。后续可升级至 v2-m3 多语言版本。

四、参数调优

RRF k 参数: 40。k 值越小排名靠前结果权重越大。向量检索权重 60%，关键词检索 40%。